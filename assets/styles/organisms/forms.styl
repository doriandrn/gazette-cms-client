.input
input
input[type="text"]
input[type="password"]
textarea
  font-size 14px
  line-height 16px
  border 0
  border: 1px solid #eee
  border-radius 6px
  transition all .15s ease-in-out
  background white
  padding 6px 10px
  color #666

  // &:focus
  //   border-color: colors.ui.primaryHover
  //   color: colors.typo.heading

  +above(l)
    font-size 12px

label
  font-size 14px
  text-transform capitalize
